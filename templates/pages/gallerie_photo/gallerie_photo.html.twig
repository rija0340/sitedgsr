
{% extends "base.html.twig" %}
{% block stylesheets %}
	<link rel="stylesheet" type="text/css" href="{{ asset( 'css/gallerie_photo/baguetteBox.min.css')}}">
	<link rel="stylesheet" type="text/css" href="{{ asset( 'css/gallerie_photo/grid-gallery.css')}}">

	{%endblock%}
	{% block content %}

		<section class="gallery-block grid-gallery">
			<div class="container">
				<div class="heading">
					<h2 class="text-center">Grid Gallery</h2>
				</div>
				{% for actualite in actualites %}
					<h5> {{ actualite.title }}</h5>
					<div class="row">
						{% for attach in actualite.attachements %}
								<div class="col-md-3 col-lg-3 item">
									<a class="lightbox" href="{{vich_uploader_asset(attach, 'imageFile')  }}">
										<img class="img-fluid image scale-on-hover" src="{{vich_uploader_asset(attach, 'imageFile')  }}">
									</a>
								</div>
						{% endfor %}

					</div>
				{% endfor %}
			</div>
		</section>
		{%endblock%}

		{% block javascripts %}
			<script src="{{ asset ('js/gallerie_photo/baguetteBox.min.js') }}"></script>
			<script>
			baguetteBox.run('.grid-gallery', { animation: 'slideIn'});
			</script>
		{% endblock %}